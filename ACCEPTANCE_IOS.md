# iPhone 實機驗收清單

> 對應 SPEC.md 第 7 節剩下的未打勾項目（M1 離線＋TTS、M2 跟讀、M3 Badge、M4 循環聽＋核心句跟讀、
> M5 每日任務／背景播放／儲存、M6 衝刺版面、**M7 多裝置同步**），
> 以及第 8 節「Safari 分頁 + 主畫面 PWA 兩種模式各測一遍」。
>
> 網址：**https://4jf4kzf2zd-cell.github.io/fabenglish/**
> 版本＝M7，service worker `VERSION = 'v8'`。
>
> ⚠️ 從舊版更新過來時，Safari 可能還抓著舊的 service worker。開始測之前先**強制重新整理一次**
> （或把 App 從主畫面刪掉重加），確認設定頁最下面寫的是 `FabEnglish · M7 · schema v4`。

驗完把結果照最後一節的格式回報，我再依實際狀況修程式或打勾 SPEC。

---

## 0　準備（約 2 分鐘）

- [ ] **0-1** iPhone Safari 開上面的網址，等到看見底部五個 tab（首頁／單字／閱讀／進度／設定）。
- [ ] **0-2** 到「進度」→ 匯出 JSON，先存一份備份（後面有「清除進度」相關的測試）。
- [ ] **0-3** 到「設定」→ 最下方「其他」→ 勾選 **開發者模式**。之後首頁底部會出現「🛠 開發者工具」時間旅行按鈕。
      （只有 localhost 會自動進 dev 模式，線上必須手動勾。）
- [ ] **0-4** 確認「設定」頁最下面那行寫的是 **「儲存空間：正常」**。
      若寫「⚠️ 無法寫入，進度不會被保存」＝你在無痕視窗，換一般視窗重來。

---

## A 輪　Safari 分頁（先跑這輪：STT 在分頁模式最穩）

### A1　TTS 首次發聲　→ SPEC M1「TTS 在 iPhone 實機可發聲（含第一次點擊解鎖）」

- [ ] **A1-1** 重新整理頁面（讓解鎖狀態歸零），直接進「設定」。
- [ ] **A1-2** 「語音」卡片第一個下拉選單，第一個選項應顯示 `自動（某個語音名稱）`，例如 `自動（Samantha）`。
      ⚠️ 顯示 `自動（無可用語音）` ＝ voices 沒載進來，記下來。
- [ ] **A1-3** 下拉選單應該列得出裝置上的英文語音，格式 `名稱（en-US） · 本機`。
- [ ] **A1-4** 按「試聽一句簡報用語」右邊的 🔊，**第一次按就要出聲**，唸的是：
      *Sort yield trended up by 2.3 points after the fix was implemented in WW32.*
      ⚠️ 第一次沒聲音、第二次才有 ＝ 解鎖失敗，是要修的 bug。
- [ ] **A1-5** 播放中再按一次 🔊 → 應立刻停止（不是疊播）。
- [ ] **A1-6** 語速拉桿只能在 **0.8×–1.2×** 之間；拉到 0.8 與 1.2 各試聽一次，聲音不應破音或變機器人。
- [ ] **A1-7** 換一個 voice（例如 en-GB）再試聽，聲音要真的換人。

### A2　長句不被截斷

- [ ] **A2-1** 到「首頁」→「模組」→ **面試常見問題** → 展開任一題 → 播放整段範答（60–90 字）。
      整段要唸完，不能唸到一半自己停掉。

### A3　跟讀 STT　→ SPEC M2「跟讀一句可得分且逐字上色正確」

- [ ] **A3-1** 「單字」→ 翻到背面 → 例句下方的跟讀面板。狀態列應為
      `直接按🎤開始唸；想先聽範讀再按「🔊 聽一次」。`，按鈕是 **`🎤 直接跟讀`**。
- [ ] **A3-2** 按 `🎤 直接跟讀`：
      1. 第一次會跳系統麥克風權限詢問 → 允許
      2. 出現 `準備跟讀⋯ 1`（約 0.7 秒）
      3. 變成 `🎤 請開始說⋯`，按鈕變 `說完了`
      ⚠️ 按下去**沒有**先播範讀才是對的（預設 `playBeforeShadow = false`）。
- [ ] **A3-3** 照著例句唸一次。唸的過程中狀態列應即時顯示辨識到的字（interim）。
- [ ] **A3-4** 唸完（或按「說完了」）後：目標句**逐字上色**，對上的綠色、沒對上的紅色；
      下方出現 `辨識到：<你唸的內容>`，以及 `綠色＝有對上、紅色＝沒對上⋯` 那行說明。
- [ ] **A3-5** 故意漏唸幾個字再試一次 → 漏掉的字要變紅。
- [ ] **A3-6** 故意多唸幾個字 → 應多出一行 `多說的字：⋯`。
- [ ] **A3-7** 按 `🎤 再唸一次` 可以重來。
- [ ] **A3-8** 到「設定」→「學習」→ 打開 **跟讀前先播一次範讀** → 回單字再跟讀一次，
      這次應先聽到範讀、播完才倒數 2 秒進錄音。**測完記得關回去**。

### A4　跟讀畫面不得出現任何分數　→ SPEC M4（不變條件 5）

- [ ] **A4-1** A3 跑完的整個畫面上，**找不到任何分數、百分比、等第、星星**。
      看到任何數字評價都是 bug，請截圖。

### A5　簡報模式與面試核心句跟讀

- [ ] **A5-1** 首頁 → **簡報句型** → 進「簡報模式」，10 句逐句跟讀走得完，中途可回上一句。
- [ ] **A5-2** 首頁 → **面試常見問題** → 任一題 → 「核心句跟讀」，跟讀的是**單句**（不是整段範答）且辨識得出來。
      → 這一項就是 SPEC M4「iPhone 實機：核心句跟讀可辨識」的一半。
- [ ] **A5-3** 面試 → **模擬面試**：抽 6 題、可自評「答得出來／卡住」、可回上一題、結束顯示卡住清單。

### A6　循環聽連續播放　→ SPEC M4「循環聽連續播放不中斷」

- [ ] **A6-1** 首頁 →「模組」→ **循環聽**。句源下拉的數量應該是：
      簡報句型（38）／Email 常用句（30）／面試關鍵句（124）／單字例句（60，還沒學過字時）／全部混合（252）。
- [ ] **A6-2** 設定「每句重複 2」「句間停頓 1」，按 `▶ 開始`。
      畫面計數應走 `1 / 38　·　第 1 次 / 共 2 次` → `第 2 次 / 共 2 次` → 跳下一句。
- [ ] **A6-3** **保持螢幕開著，連續播 3 分鐘不要碰手機**，確認不會自己停掉。
      （這是最關鍵的一項——iOS 的 speechSynthesis 在長時間連播容易靜默死掉。）
- [ ] **A6-4** ⏮ / ⏭ 可跳句，播放中跳句要接著繼續播。
- [ ] **A6-5** `⏸ 暫停` 要立刻靜音。
- [ ] **A6-6** 切到「顯示中／已隱藏」「照順序／隨機」都要即時生效。
### A6b　關螢幕繼續播（M5，重點）

> 這是「盡力而為」的功能：iOS 沒有正式支援網頁背景朗讀。**三種結果都要如實記下來**，
> 不要只寫「不行」——②和③對後續怎麼修的差別很大。

- [ ] **A6b-1** 循環聽頁面下方應有 **關螢幕繼續播｜嘗試** 的開關（預設「嘗試」）。
- [ ] **A6b-2** 按 `▶ 開始` 播放中，**按側鍵把螢幕關掉，等 2 分鐘**，然後看結果屬於哪一種：
      **① 一直在唸** → 成功，記「①」
      **② 沒聲音，但鎖定畫面（或控制中心）有 FabEnglish 的播放控制** → 記「②」
      **③ 沒聲音，鎖定畫面什麼都沒有** → 記「③」
- [ ] **A6b-3** 若是 ② ：鎖定畫面上的 ⏭ / ⏮ / ⏸ 按下去有沒有反應？（解鎖後看句子有沒有換）
- [ ] **A6b-4** 解鎖回到 App：若剛剛被中斷，畫面上應出現 `⚠️ 剛剛被系統中斷了，已從這一句接著播。`
      並且**自動接著唸下去**（不需要你再按一次播放）。
- [ ] **A6b-5** 把開關切成「不嘗試」→ 提示文字要變成 `關螢幕會停止朗讀（已關閉背景播放嘗試）。`
- [ ] **A6b-6** 整個過程中**不應該聽到任何雜音或嗡嗡聲**（背景用的音軌是刻意做到幾乎無聲的）。
      若有聽到雜音，這是 bug，請記下來。

### A6c　原本的鎖屏行為

- [ ] **A6-7** 若 A6b-2 的結果是 ② 或 ③：解鎖後按 `▶ 開始` 要能繼續，不會壞掉。

### A7　聽力模組

- [ ] **A7-1** 首頁 → **聽力** → 任一段對話：兩個 speaker 用不同語音（en-US / en-GB），聽得出差別。
- [ ] **A7-2** 語速 0.8× / 1.0× / 1.2× 三檔都能切且不失真；單句可重播；字幕可顯示／隱藏。
- [ ] **A7-3** 四階段（盲聽 → 理解題 → 開字幕重聽 → 聽寫題）之間可以**回上一步**，已作答的內容還在。
- [ ] **A7-4** 聽寫題輸入 `92.5% → 87.1%, WW32` 這類自由格式要判得對。

### A8　時間旅行（隔日複習）

- [ ] **A8-1** 「單字」學 3 個新字 → 首頁「🛠 開發者工具」按 `+1 天` → 這 3 個字要出現在複習佇列。
- [ ] **A8-2** 按 `歸零` 復原。

### A9　每日任務（M5，重點）

- [ ] **A9-1** 一打開首頁，最上面就是 **今日任務** 卡片：右上角 `0 / 3`、一條進度條、三列任務。
      第一列一定是「清完今日單字 N 張」。
- [ ] **A9-2** 三列任務的右邊各顯示 `x / y` 進度；點任一列會跳到對應的模組。
- [ ] **A9-3** 按卡片下方的 **繼續** → 應直接跳到「第一個還沒完成」的模組。
- [ ] **A9-4** 去做完其中一項（例如「聽 1 段對話」把理解題答完）→ 回首頁，那一列應該變成
      **綠色 ✓ ＋ 文字劃掉**，右上角變成 `1 / 3`，進度條前進。
- [ ] **A9-5** **同一篇文章再讀一次 / 同一句再唸一次 / 同一題再自評一次，數字不應該再加**。
      （會重複加就是 bug，這是刻意擋掉的。）
- [ ] **A9-6** 三項全部做完 → 標題變成 `今天做完了 ✓`，「繼續」按鈕消失。
- [ ] **A9-7** 今日任務卡下面有 **最近 14 天** 的 14 個小方格，有練的日子是深色，今天那格有外框。
- [ ] **A9-8** 首頁「🛠 開發者工具」按 `+1 天` → 任務組合應該換一套（每天的第二、三項不同）。測完按 `歸零`。

---

## B 輪　加到主畫面（PWA standalone）

### B1　安裝

- [ ] **B1-1** Safari 分享選單 → 加入主畫面。圖示是 FabEnglish 圖示、名稱顯示 **FabEnglish**。
- [ ] **B1-2** 從主畫面開啟：**沒有 Safari 網址列**（standalone），狀態列不會蓋住標題。
- [ ] **B1-3** A 輪存的進度還在（同一個 origin 的 localStorage）。

### B2　離線可用　→ SPEC M1「離線可開啟（除 STT 外）」

- [ ] **B2-1** 先在**有網路**時把六個模組各開一次（讓 content/*.json 進 cache）。
- [ ] **B2-2** 開飛航模式 → 從主畫面開啟 App。
      應正常開起來、首頁數字正確、單字／閱讀／Email／簡報／聽力／面試／循環聽的內容都讀得到。
- [ ] **B2-3** 離線下 TTS（🔊 播放）**仍要能發聲**。
- [ ] **B2-4** 離線下切換頁面不會出現「離線且沒有快取」。

### B3　離線時跟讀顯示提示而不是壞掉　→ SPEC M2

- [ ] **B3-1** 仍在飛航模式，打開任一跟讀面板。狀態列應顯示：
      `🎤 跟讀功能需要網路連線（辨識在 Apple 伺服器上做）。`
      而且**不會**出現 `🎤 直接跟讀` 按鈕（只留 `🔊 聽一次`）。
- [ ] **B3-2** 關掉飛航模式、重新整理 → 跟讀按鈕回來。

### B4　App 圖示待複習數字　→ SPEC M3「圖示數字會出現」

> 需要 iOS 16.4 以上，而且一定要從**主畫面圖示**開啟才算數。

- [ ] **B4-1** 從主畫面開 App →「設定」→「提醒」卡片。
      若顯示標題 `未完成任務數字提醒` ＋ 一段說明文字 ＝ 這台 iOS 不支援，此項直接記「不支援」跳過。
- [ ] **B4-2** 勾選 **在 App 圖示顯示今日未完成任務數** → 系統跳通知權限 → 允許。
      狀態文字應變成 `已開啟。數字＝今日還沒完成的任務數（做完就消失）。`
- [ ] **B4-3** 回主畫面看 FabEnglish 圖示 → 右上角數字應**等於首頁今日任務還沒完成的項數**
      （首頁顯示 `1 / 3` 時，圖示應該是 **2**）。
- [ ] **B4-4** 把今天三項全部做完 → 回設定按「重新整理圖示數字」→ 圖示數字應消失。
- [ ] **B4-5** 取消勾選 → 數字消失。

### B5　standalone 下的 STT（已知風險）

- [ ] **B5-1** 從主畫面開的 App 裡跟讀一句。
      **成功** → 記「standalone STT 可用」。
      **失敗** → 畫面應顯示
      `無法啟動語音辨識。從主畫面開啟時較舊的 iOS 有此問題，請改用 Safari 分頁開啟。`
      或 `系統不允許語音辨識。若是從主畫面開啟，請改用 Safari 分頁開啟再試一次。`
      → 這是**已知的 iOS 限制**（SPEC §5-8），只要提示有出現就算通過，不是 bug。
      ⚠️ 只有「畫面卡住、一直轉、或什麼都沒說」才是 bug。

### B6　版面

- [ ] **B6-1** 直向：底部 tab bar 不被 home indicator 蓋住，所有按鈕單手按得到。
- [ ] **B6-2** 橫向：不破版、不出現水平捲軸。
- [ ] **B6-3** 深色模式（設定 → 顯示與亮度 → 深色）下配色正常、文字讀得到。

### B7　儲存空間 fallback

- [ ] **B7-1** Safari 無痕分頁開網址 → 設定頁「儲存」卡片應顯示
      `⚠️ 無法寫入（無痕模式？），這次的進度不會被保存。`，而不是整個 App 壞掉。

### B8　進度保存（M5）

> 進度一直都存在這台手機的瀏覽器裡。這一組要驗的是「iOS 會不會把它清掉」的防護。

- [ ] **B8-1** 從主畫面開 App →「設定」→「儲存」卡片，應寫著
      `全部進度都存在這台裝置的瀏覽器裡（localStorage）⋯`，狀態列顯示 `狀態：正常`，並顯示用量 KB 數。
- [ ] **B8-2** 按 **要求長期保存** → 狀態應變成 `已標記為長期保存 ✓`（按鈕跟著變灰）。
      若一直維持「尚未標記」，記下來 —— Safari 有時會拒絕，不算 App 的 bug，但要知道。
- [ ] **B8-3** 完全關掉 App（從多工上滑移除）再開 → 今日任務進度、單字排程都還在。
- [ ] **B8-4** 到「進度」→ 匯出 JSON → 用純文字開啟，確認裡面有 `"schemaVersion": 4` 與 `"daily"` 欄位，
      而且**搜尋 `token` 應該找不到**（登入資訊不可以夾在備份裡）。
- [ ] **B8-5**（長期）**放著 7 天不要開**，第 8 天再打開看進度還在不在。
      這一項只能靠時間，不用現在做，記得回報結果就好。

---

## C 輪　面試衝刺（M6）

### C1　六週地圖與倒數

- [ ] **C1-1** 首頁最上面有一條倒數列：`距離面試 N 天 ｜ 第 M / 42 天 ｜ W? 焦點`，數字對得上。
- [ ] **C1-2** 點倒數列進 `#/sprint`：六週地圖不破版（390px 寬），今天所在的那一格有底色。
- [ ] **C1-3** 「本週節奏」的膠囊（面試／閱讀／聽力／Email／跟讀／循環聽）沒有擠成兩行難讀。
- [ ] **C1-4** 用開發者工具的時間旅行 +1 天 → 回首頁，倒數少 1、任務換成隔天那三項。
- [ ] **C1-5** 在 `#/sprint` 把面試日改早一週 → 課表應該**往後跳**（不是停在第 1 天）。

### C2　對話練習（`#/interview/talk`，前三週的主力）

- [ ] **C2-1** 首頁今日任務點「對話練習 3 題」進去，第一輪會唸出題目（TTS 有聲音）。
- [ ] **C2-2** 按「答完了，繼續追問 →」會換到下一個追問，題目**每一輪都不一樣**（共四輪）。
- [ ] **C2-3** 每一輪都能按「偷看回答方向」，展開後看得到中文提示。
- [ ] **C2-4** 四輪跑完出現自評（👍／🤔）＋範答；自評完回首頁，面試那一項的數字 +1。
- [ ] **C2-5** 同一題再自評一次，數字**不會**再 +1。

---

## D 輪　帳號與多裝置同步（M7，重點）

> 這一輪要兩台裝置：iPhone ＋ 任何一台電腦／平板的瀏覽器都行。
> **最大的未知數是 D2**——iOS 把網站加到主畫面之後，Google 的登入彈窗有可能被系統擋掉。
> 擋掉不是 bug、也不影響功能（走 D3 的配對碼就好），但我需要知道結果才能決定文案怎麼寫。

### D1　沒登入時什麼都不該變

- [ ] **D1-1** 全新安裝、完全不碰帳號功能，練單字／閱讀／聽力一輪，行為和以前完全一樣。
- [ ] **D1-2** 「設定」最上面的「帳號」卡片寫著 `尚未同步`，說明是「不登入也完全可以用」。
- [ ] **D1-3** 開飛航模式 → App 照常能練、沒有任何錯誤提示跳出來。

### D2　Google 登入（Safari 分頁 ＋ 主畫面 PWA 各測一次）

> 前提：我需要你先照 `worker/README.md` 建一組 OAuth 用戶端 ID 給我，這一組才能測。

- [ ] **D2-1** Safari 分頁：設定 → 帳號 → `設定同步` → 按 Google 按鈕 → 選帳號 → 回到 App 顯示你的信箱。
- [ ] **D2-2** 主畫面 PWA：同樣流程。**這裡最可能失敗**，請回報是哪一種：
      ① 正常完成　② 彈窗開了但登入完回不到 App　③ 彈窗根本開不起來　④ 其他（描述）
- [ ] **D2-3** 登入後「立即同步」按下去，狀態列變成 `最後同步：今天 HH:MM`。

### D3　配對碼（不用 Google 的路，一定要能通）

- [ ] **D3-1** iPhone：帳號頁 → `把這台裝置的進度開始同步` → 狀態變成已連線。
- [ ] **D3-2** iPhone：`產生配對碼` → 出現 8 碼（XXXX-XXXX），字夠大、抄得下來。
- [ ] **D3-3** 電腦開同一個網址 → 帳號頁 → 輸入那組碼 → `用配對碼加入`。
- [ ] **D3-4** 電腦上應該立刻看得到 iPhone 練過的進度（單字數、streak 對得上）。

### D4　雙向同步（這一輪的重點）

- [ ] **D4-1** 電腦上練 5 個單字 → 等 5 秒 → iPhone 上把 App 完全關掉再開 → 那 5 個字的排程在。
- [ ] **D4-2** 反過來：iPhone 練一篇閱讀 → 電腦重新整理 → 閱讀紀錄在。
- [ ] **D4-3** 兩邊**同時**各練幾個不同的字（不要等同步）→ 各自重開 → **兩邊的字都在，沒有一邊被吃掉**。
- [ ] **D4-4** 在電腦上改「每日新字上限」→ iPhone 重開後設定跟著變。
- [ ] **D4-5** 在 iPhone 上開／關面試衝刺 → 電腦重開後跟著變。

### D5　斷線與異常

- [ ] **D5-1** iPhone 開飛航模式 → 練幾個字 → 關飛航模式 → 進度會自動補傳（電腦重開看得到）。
- [ ] **D5-2** 帳號頁按 `中斷同步` → 本機進度**完全沒少**，狀態回到未同步。
- [ ] **D5-3** 再用配對碼接回去 → 進度沒有重複、沒有變少。

---

## 回報格式

逐項回報最省事的寫法（只列有問題的就好，其餘寫「其他全過」）：

```
A1-4 ✅
A1-2 ❌ 顯示「自動（無可用語音）」，等了 5 秒還是一樣
A6-3 ❌ 播到第 7 句就沒聲音了，畫面計數還在跑
B4-3 ⚠️ 圖示沒數字，但設定頁顯示「已開啟」
B5-1 ✅（有出現「請改用 Safari 分頁」提示）
其他全過
```

有畫面問題直接截圖給我。這輪最重要的三項：

1. **A6b-2（關螢幕繼續播）** —— 這是新做的功能，而且 iOS 不保證吃。記得回報 ①／②／③ 是哪一種。
2. **A6-3（循環聽連播 3 分鐘）** —— iOS 語音引擎長時間連播會靜默死掉的老問題。
3. **A1-4（首次點擊就要發聲）** —— TTS 解鎖失敗的話整個 App 都不會出聲。

其他都是靜態行為，出錯的機率低很多。

---

## 全部通過之後

我會把 SPEC.md 第 7 節這些 checkbox 打勾並 commit：

- M1 `iPhone Safari 開啟 GitHub Pages 網址，可加入主畫面，離線可開啟` ← B1 + B2
- M1 `TTS 在 iPhone 實機可發聲（含第一次點擊解鎖）` ← A1
- M2 `iPhone 實機跟讀一句可得分且逐字上色正確` ← A3 + A4
- M3 `iPhone 實機確認圖示數字會出現` ← B4
- M4 `循環聽連續播放不中斷、核心句跟讀可辨識` ← A6 + A5-2
- M5 `鎖屏後循環聽是否繼續播、鎖定畫面是否出現播放控制` ← A6b
- M5 `加到主畫面後 7 天不開，進度是否還在` ← B8-5（要等一週）
- M6 `衝刺卡片與六週地圖在 390px 寬不破版` ← C1
- M7 `Google 登入實際跑得通` ← D2
- M7 `PWA 裡的 Google 登入彈窗會不會被 iOS 擋掉` ← D2-2（擋掉就走配對碼，不算失敗）
